using namespace std;
#include <algorithm>
#include <iostream>

int main() {
    int a, b;
    cin >> a >> b;
    string arrA[a][a];

    for (int i = 0;i < a;i++) {
        for (int r = 0;r < a;r++) {
            cin >> arrA[i][r];
        }
    }
    for (int i = 0;i < a;i++) {
        for (int r = (i + 1);r < a;r++) {
            swap(arrA[i][r], arrA[r][i]);
            }
        }

    for (int i = 0;i < a;i++) {
        for (int r = 0;r < a;r++) {
            cout << arrA[i][r] << " ";
        }
        cout << endl;
    }
    return 0;
}
